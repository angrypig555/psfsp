# Pretty Simple File Sharing Protocol
how it would work:
client sends a request to the server on port 6434, the server responds with an ephemeral port to do the file transfer operation, uses hashing to confirm file integrity